#include "main.h"

/**
 * _isdigit - check if the number is b/n 0 to 9
 * @c: input no
 * Return: 1 if it is a number (0 to 9),0 in other case
 */

int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
retrun (1);
}
else
{
return (0);
}
}
